<%= form_with(model: urgency_level, local: true) do |form| %>
  <% if urgency_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(urgency_level.errors.count, "error") %> prohibited this urgency_level from being saved:</h2>

      <ul>
      <% urgency_level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :level_description %>
    <%= form.text_field :level_description, id: :urgency_level_level_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
