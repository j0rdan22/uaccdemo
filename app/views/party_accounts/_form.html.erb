<%= form_with(model: party_account, local: true) do |form| %>
  <% if party_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(party_account.errors.count, "error") %> prohibited this party_account from being saved:</h2>

      <ul>
      <% party_account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :party_id %>
    <%= form.collection_select :party_id, Party.all,:id, :first_name %>
  </div>

  <div class="field">
    <%= form.label :account_id %>
    <%= form.collection_select :account_id, Account.all, :id, :account_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
