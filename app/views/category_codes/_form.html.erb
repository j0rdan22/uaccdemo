<%= form_with(model: category_code, local: true) do |form| %>
  <% if category_code.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category_code.errors.count, "error") %> prohibited this category_code from being saved:</h2>

      <ul>
      <% category_code.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code_description %>
    <%= form.text_field :code_description, id: :category_code_code_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
